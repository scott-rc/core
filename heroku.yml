release:
  command:
    - mkdir -p bin/database/migrations
    - cp template/graphql.schema bin/graphql.schema
    - cp -R template/database/migrations bin/database/migrations
    - cd template/src; go build -o ../../bin/app
run:
  web: ./bin/app